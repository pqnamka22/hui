# Основные зависимости
aiogram==3.3.0
aiohttp==3.9.1
Pillow==10.1.0
python-dotenv==1.0.0

# Для работы с изображениями
Pillow==10.1.0
numpy==1.24.3  # Для сложной обработки изображений (опционально)
opencv-python==4.8.1.78  # Для крутых эффектов (опционально)

# Для базы данных и кэширования
redis==5.0.1  # Для быстрого кэширования (опционально)
asyncpg==0.29.0  # Для PostgreSQL (опционально)
pymongo==4.5.0  # Для MongoDB (опционально)
sqlalchemy==2.0.23  # Для ORM (опционально)

# Для генерации градиентов и эффектов
colour==0.1.5
gradient==0.0.4
noise==1.2.2  # Для текстур (опционально)

# Для работы с криптой и API
pycryptodome==3.19.0
cryptography==41.0.7
requests==2.31.0
beautifulsoup4==4.12.2  # Для парсинга (опционально)

# Для логирования и мониторинга
loguru==0.7.2
sentry-sdk==1.35.0  # Для отслеживания ошибок (опционально)
prometheus-client==0.19.0  # Для метрик (опционально)

# Утилиты
python-dateutil==2.8.2
pytz==2023.3
tzlocal==5.2
psutil==5.9.6  # Мониторинг ресурсов

# Для генерации QR-кодов (для платежей)
qrcode[pil]==7.4.2

# Для работы с Excel/CSV (экспорт статистики)
pandas==2.1.3  # Тяжелая, но мощная
openpyxl==3.1.2
xlrd==2.0.1

# Для тестирования
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Для деплоя и Docker
gunicorn==21.2.0  # Для веб-сервера
uvicorn[standard]==0.24.0  # ASGI сервер
docker==6.1.3  # Для управления Docker

# Для безопасности
bcrypt==4.1.1
passlib==1.7.4
python-jose[cryptography]==3.3.0  # JWT токены
